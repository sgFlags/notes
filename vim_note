n== (n为行数）可以将几行代码格式化

2.  shift + a : move the cursor to the end of line and into insert mode (in normal node)
    shift + i : move the cursor to the start of line and into insert mode (in normal mode)


设置将vim中的tab缩进设置为4
http://www.cnblogs.com/wi100sh/p/4938996.html
在按照说明修改后需要将expandtab改为noexpandtab
